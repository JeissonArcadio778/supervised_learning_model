from optimal_route import optimal_route
from supervised_learning_model import supervised_learning_model


def main():
    # Llamamos a la función para encontrar la mejor ruta
    print(optimal_route())

    # Llamamos a la función para entrenar un modelo de aprendizaje supervisado
    print(supervised_learning_model())


if __name__ == "__main__":
    main()
